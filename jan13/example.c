#include <stdio.h>

int main()
{
    // fprintf() vs. printf()
    // operators
    // if statements
    // for loops and while loops

    printf("%c\n", 1 + c);
    printf("%d\n", 1 + c);

    int n = 1, p = 2;

    n += p;
    n = p++ + 2;
    n = ++p + 2;

    int i = 0;

    for (i = 1; i <= 10; i -= 2)
    {
        ;
    }

    return 0;
}