#include <stdio.h>

int main()
{
    // quicksort
    // set one elem to be a pivot
    // move other elements to the left/right
    // set pivot and 2 ptrs
    // left ptr: find elems >pivot
    // right ptr: find elems <pivot
    // ptrs move closer and when 2 elems are found, we swap
    // do this until index cross over
    // swap pivot to final destination
    // pivot will be in correct position
    // repeat on left and right sections

    // nlogn (best) n^2 (worst)

    return 0;
}