#include <stdio.h>

int main()
{
    // open file
    // FILE *fopen(const char *filename, const char *mode)
    // mode = "r"/"w"/"a"

    // close file
    // int fclose(FILE, *stream)

    return 0;
}